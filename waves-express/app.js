const P=window.WAVES_PRODUCTS,grid=document.querySelector('#productGrid'),filters=document.querySelector('#filters');const cats=['Todos',...new Set(P.map(p=>p.category))];function money(v){return v==null?'Consultar':v.toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}function img(p){return `assets/products/${p.images[0]}`}function wa(p){const t=`Olá! Quero comprar pelo Waves Express: ${p.name}. Código: ${p.code}. Pode me atender?`;return `https://wa.me/558530318830?text=${encodeURIComponent(t)}`}function render(cat='Todos'){grid.innerHTML=P.filter(p=>cat==='Todos'||p.category===cat).map(p=>`<article class="card"><div class="card-image"><span class="badge">${p.category}</span><img src="${img(p)}" alt="${p.name}" loading="lazy"></div><div class="card-body"><div class="code">CÓD. ${p.code}</div><h3>${p.name}</h3><p>${p.composition}</p><div class="card-actions"><a class="details" href="produto.html?id=${p.id}">Ver detalhes</a><a class="buy" href="${wa(p)}" target="_blank" rel="noopener">Quero comprar</a></div></div></article>`).join('')}filters.innerHTML=cats.map((c,i)=>`<button class="${i===0?'active':''}" data-cat="${c}">${c}</button>`).join('');filters.addEventListener('click',e=>{if(!e.target.matches('button'))return;filters.querySelectorAll('button').forEach(b=>b.classList.remove('active'));e.target.classList.add('active');render(e.target.dataset.cat)});render();